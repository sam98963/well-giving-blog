<nav class="bg-gray-100">
  <div class="container mx-auto p-4">
    <div class="flex justify-between items-center">
      <a class="text-xl font-semibold text-gray-800" href="/">Navbar</a>
      <ul class="flex space-x-4">
        <% if current_user %>
          <li>
            <%= link_to "Edit Account", edit_user_registration_path, class: "text-blue-500 hover:text-blue-700" %>
          </li>
          <li>
            <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "px-4 py-2 text-white bg-blue-500 hover:bg-blue-700 rounded" %>
          </li>
        <% else %>
          <li>
            <%= link_to "Sign Up", new_user_registration_path, class: "text-blue-500 hover:text-blue-700" %>
          </li>
          <li>
            <%= link_to "Sign In", new_user_session_path, class: "text-blue-500 hover:text-blue-700" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
